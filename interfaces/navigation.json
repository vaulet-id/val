{
  "_comment": "Moving between screens. The parser knows none of these words: `replace`, `sheet` and `root` are read from here, so a host without sheets simply does not list `present`, and asking for one is the same message as asking for a component it does not have.",
  "capability": "navigation",
  "version": 1,

  "operations": {
    "navigate": {
      "props": { "to": "Screen", "as": "Transition?", "present": "Present?" },
      "reversible": true
    },
    "back": {
      "props": { "with": "Record?" },
      "reversible": true
    }
  },

  "screen": {
    "props": {
      "start": "bool?",
      "present": "Present?",
      "address": "Address?"
    }
  },

  "vocabularies": {
    "transition": ["push", "replace", "root"],
    "present": ["full", "sheet"]
  }
}
